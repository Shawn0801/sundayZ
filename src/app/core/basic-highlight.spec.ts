import { BasicHighlight } from './basic-highlight';

describe('BasicHighlight', () => {
  it('should create an instance', () => {
    const directive = new BasicHighlight();
    expect(directive).toBeTruthy();
  });
});
